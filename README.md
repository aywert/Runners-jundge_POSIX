Judge-Runners - is a programm which simulates a real race using IPC POSIX. Judge and runners are seperate processes. Judge gives a start to a first runner, then first runners gives a "stick" to the next.